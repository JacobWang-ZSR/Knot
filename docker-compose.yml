services:
  knot:
    container_name: knot
    image: jacobwangzsr/knot:latest
    build: .
    ports:
      - "8081:8081"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - JSON_PATH=/app/data/passwords.jason