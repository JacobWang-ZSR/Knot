version: '3.8'

services:
  knot:
    build: .
    ports:
      - "8081:8081"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - JSON_PATH=/app/data/passwords.jason